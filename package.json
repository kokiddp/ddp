{
  "name": "ddp",
  "version": "0.0.1",
  "private": true,
  "description": "Dynamic Destiny Protocol — open, self-hostable RPG session framework",
  "author": "ELK-Lab",
  "license": "MIT",
  "engines": {
    "node": ">=20",
    "pnpm": ">=10"
  },
  "packageManager": "pnpm@10.30.3",
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "build": "pnpm -r run build",
    "lint": "pnpm exec eslint .",
    "lint:fix": "pnpm exec eslint . --fix",
    "typecheck": "pnpm --filter @ddp/shared-types run build && pnpm --filter @ddp/shared-rules run build && pnpm -r run typecheck",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:integration": "vitest run tests/integration",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@ddp/eslint-config": "workspace:*",
    "eslint": "^9.39.0",
    "pinia": "^2.2.0",
    "prettier": "^3.3.0",
    "typescript": "^5.6.0",
    "vitest": "^4.0.18",
    "vue": "^3.5.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "msgpackr-extract",
      "vue-demi"
    ]
  }
}
